<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <!--
        * @bainloko
        * PROGRAMAÇÃO IV
        * 22/11/2021
    -->
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <meta name="theme-color" content="#ffffff">
    <title>Painel Administrativo</title><style>
        body {
            margin: 2em;
            font-family: Arial, Helvetica, sans-serif;
            text-align: justify;
        }

        a:hover, span:hover {
            color: blue;
        }

        form {
            width: 600px;
            padding: 1.1em;
            border: 1px solid #CCC;
            border-radius: 1em;
        }

        h3 {
            margin-top: -1px;
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        label {
            width: 90px;
            display: inline-block;
            text-align: right;
            font-size: 21px;
        }

        input {
            border: 1px solid #999;
            margin-top: 3px;
            width: calc(100% - 155px);
            font-size: 14px;
            display: inline-block;
        }

        input:focus {
            border-color: #000;
        }

        #fotoInp {
            width: calc(100% - 152px);
        }

        #addInp {
            width: 100%;
            margin: 1 auto;
            display: flex;
        }

        .obrigatorio {
            color: red;
            font-weight: 1000;
        }

        span {
            padding: 5px;
            display: inline-block;
        }

        #acoes {
            text-decoration: none;
        }
    </style>
</head>
<body>
    <nav>
        <span><a href="/admin/usuarios">Para ir à listagem dos usuários...</a></span><span><a href="/admin/usuarios/add">Para adicionar um usuário...</a></span><span><a href="/admin">Voltar para a página inicial</a></span> 
    </nav>
    <header>
        <h1>Editar Usuário</h1>
    </header>
    <article>
        <form method="post" enctype="multipart/form-data">
            <h3><%=usuario.nome%></h3>
            <h3>Nos campos onde há '<span class="obrigatorio">*</span>', o preenchimento é obrigatório!</h5>
            <ul>
                <li>
                    <label>ID:&ensp;</label><input type="text" name="id" readonly value="<%=usuario.id%>" />
                </li>
                <li>
                    <span class="obrigatorio">*</span><label>Nome:&ensp;</label><input type="text" name="nome" value="<%=usuario.nome%>" required />
                </li>
                <li>
                    <span class="obrigatorio">*</span><label>E-mail:&ensp;</label><input type="email" name="email" value="<%=usuario.email%>" required />
                </li>
                <li>
                    <span class="obrigatorio">*</span><label>Senha:&ensp;</label><input type="password" name="senha" value="<%=usuario.senha%>" required />
                </li>
                <li>
                    <img src="/uploads/usuarios/<%=usuario.foto%>" alt="foto de algum usuário" width="140px" />
                    <br>
                    <span class="obrigatorio">*</span><label>Foto:&ensp;</label><input id="fotoInp" type="file" name="foto" required />
                    <input id="addInp" type="submit" value="Adicionar Usuário" />
                </li>
            </ul>
        </form>
        <br>
        <hr>
        <h3 id="mensagem">
            <% if (msg){ %>
            <%= msg %>
            <% } else { %>
            <%= "" %>
            <% } %>
        </h3>
    </article>
    <footer>
        <br>
        <h3>© Kauã Maia, 2021 - Contato <a href="https://github.com/bainloko">github.com/bainloko</a></h3>
    </footer>
</body>
</html>